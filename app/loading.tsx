/**
 * La app es principalmente cliente en `page.tsx`; un overlay aquí
 * duplicaba la sensación de “siempre cargando”. El feedback fino va en cada vista.
 */
export default function Loading() {
  return null
}
